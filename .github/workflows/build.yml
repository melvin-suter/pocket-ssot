name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: pocket-ssot.frontend/package-lock.json

      - name: Install npm dependencies
        run: npm ci
        working-directory: pocket-ssot.frontend

      - name: Install build deps (fpm + rpm tooling)
        run: |
          sudo apt-get update
          sudo apt-get install -y ruby ruby-dev gcc make rpm
          sudo gem install --no-document fpm

      - name: Build packages
        run: |
          chmod +x ./build.sh
          ./build.sh

      - name: Collect artifacts
        run: |
          mkdir -p dist
          # fpm outputs into current directory by default
          mv out/*.deb dist/ || true
          mv out/*.rpm dist/ || true
          cp out/cli/pocket-ssot dist/ || true
          ls -lah dist

      - name: Create GitHub Release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
