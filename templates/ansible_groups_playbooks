{{- range $entity := .entities }}
{{- $fields := get $entity "fields" }}
{{- $roles := (index $fields "roles") | default (list) }}
{{- $build_playbook := (index $fields "build_playbook") | default (false) }}

{{- if $build_playbook }}
- name: SSOT - {{ index $entity "name" }}
  hosts: {{ index $fields "groupname" }}
  vars:
    motd_job: "{{ index $entity "name" }}"
  roles:
{{- range $g := $roles }}
  - {{ $g }}
{{- end }}
{{- end }}
{{ end }}