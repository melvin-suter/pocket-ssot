{{ $fields := get .entity "fields" }}
{{ $children := (index $fields "children") | default (list) }}

{{ index $fields "groupname" }}:
  vars:
{{- with (index $fields "groupvars") }}
{{ . | toString | nindent 4 }}
{{- end }}
  children:
{{- range $g := $children }}
    {{ $g }}:
{{- end }}

